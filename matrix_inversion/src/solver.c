#include "solver.h"

/* ----------------------------------------------------------- */

void inverse_matrix(struct block_matrix *matrix,
                    struct block_matrix *result)
{
  UNUSED(matrix);
  UNUSED(result);

}

/* ----------------------------------------------------------- */
