#include "solver.h"

/* ----------------------------------------------------------- */

void inverse_block_matrix(struct block_matrix *matrix,
                          struct block_matrix *result)
{
  UNUSED(matrix);
  UNUSED(result);

}

/* ----------------------------------------------------------- */
