#include "test_solver.h"

/* ----------------------------------------------------------- */

enum error_type
inverse_matrix(struct simple_matrix *matrix, struct simple_matrix *result)
{
  UNUSED(matrix);
  UNUSED(result);
  return ET_CORRECT;
}

/* ----------------------------------------------------------- */
